-- Hand-crafted: hai câu trên MỘT dòng (câu vô hại + `;` + câu ghi schema ngoài đích).
-- Guard cũ neo động từ ở đầu DÒNG nên bỏ lọt câu sau `;`. Đích `app` phải TỪ CHỐI.
SET statement_timeout = 0;
SELECT 1; DROP TABLE _supavisor.tenants;
